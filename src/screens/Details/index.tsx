import React from 'react';
import { View, Text } from 'react-native';

const Details = (): React.FunctionComponentElement<{}> => {
  return (
    <View>
      <Text>HELLO Details</Text>
    </View>
  );
};

export default Details;
